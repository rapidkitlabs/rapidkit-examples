# Migration Surface

This example currently uses an empty migration baseline. Add Prisma, TypeORM,
or another runtime-native migration tool here when the service starts owning
persistent schema changes.
