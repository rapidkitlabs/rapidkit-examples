


version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: saas-admin:latest
    env_file:
      - .env
    ports: ["8001:8001"]
    command: "poetry run uvicorn src.main:app --host 0.0.0.0 --port 8001"
    volumes:
      - .:/app
    restart: unless-stopped

  postgres:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_DB: saas-admin
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data


volumes:
  postgres-data:
    driver: local

